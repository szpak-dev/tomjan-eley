---
const { socials } = Astro.props;

type SocialProps = {
    name: string;
    url: string;
    icon: string;
}
---

<div class="d-flex gap-3 bg-dark">
{socials.map((social: SocialProps) => (
    <a href={social.url} class="text-white">{social.name}</a>
))}
</div>
